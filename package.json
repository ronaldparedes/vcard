{
  "name": "react-vcard-creator",
  "version": "1.0.0",
  "description": "Animated form fields using syled-components. Simple form validation logic. Business card display with interactive fields and downloadable VCard v3.",
  "keywords": [],
  "homepage": "https://www.ronaldparedes.com",
  "main": "src/index.js",
  "dependencies": {
    "file-saver": "2.0.2",
    "gh-pages": "^2.2.0",
    "react": "16.12.0",
    "react-dom": "16.12.0",
    "react-scripts": "3.3.1",
    "styled-components": "5.0.1"
  },
  "devDependencies": {},
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
